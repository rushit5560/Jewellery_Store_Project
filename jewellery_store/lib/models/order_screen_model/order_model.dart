class OrderInfo {
  final imgUrl;
  final title;
  final activePrice;
  final inActivePrice;
  final itemCount;

  OrderInfo({this.imgUrl, this.title, this.activePrice, this.inActivePrice,
    this.itemCount});
}